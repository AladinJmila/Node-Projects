{{#each ideas}}
  <div class='card card-body mb-2'>
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <a href='/ideas/edit/{{_id}}' class='btn btn-dark btn-block'>Edit</a>
  </div>
{{else}}
  <p>There are no video ideas in db</p>
{{/each}}